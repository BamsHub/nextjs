import LoginPage from '@/sections/login/LoginPage';

export const metadata = {
    title: 'Login — CoffeeChain',
    description: 'Masuk ke dashboard blockchain kopi Indonesia',
};

export default function LoginRoute() {
    return <LoginPage />;
}
